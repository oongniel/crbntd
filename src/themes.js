import { colors } from './variables';

export const themes = {
    muiTheme: {
        palette: {
            primary1Color: `${colors.blue[0]}`,
            accent1Color: `${colors.secondary[0]}`,
        },
        appBar: {
            color: `${colors.blue[0]}`,
        },
    }
};
